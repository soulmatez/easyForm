{
  "name": "esayform",
  "version": "1.0.1",
  "description": "以uView为UI框架，使用JSON数据搭建表单页面，实现零代码插入自己想要的业务场景，帮助开发者专注于数据逻辑层和业务层",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "vue-cli-service build",
    "lint": "vue-cli-service lint",
    "package": " vue-cli-service build --target lib ./lib/index.js --name easyform --dest easyform"
  },
  "keywords": [
    "easy、uView、form"
  ],
  "author": "soulmate",
  "license": "ISC"
}
